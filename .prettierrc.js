/** @type {import("prettier").Options} */
const config = {
  trailingComma: 'es5',
  tabWidth: 2,
  semi: false,
  singleQuote: true,
  jsxSingleQuote: true,
  useTabs: false,
  printWidth: 80,
  // plugins: [require('prettier-plugin-tailwindcss')],
}

module.exports = config
